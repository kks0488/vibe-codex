[interface]
display_name = "VC Phase Loop"
short_description = "End-to-end execution: plan → execute → test → prove."
brand_color = "#EF4444"
default_prompt = "Execute end-to-end using sub-agents for parallel recon/testing, with retries and completion proof."
